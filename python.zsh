#
# Python specific scripts and aliases
#

# run web server on arbitrary file
add_alias webs "python -m SimpleHTTPServer" "Run a web server on the current directory or a specified file"

# Output a list of aliases and functions that have been loaded from this file
# echo "[loaded:python] webs"
